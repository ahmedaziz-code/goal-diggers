val prefrences =getsharedprefrences("database", "Context.MODE_PRIVET")
val savedmsg= prefrences.getstring("savedgettext", "this value doesn't exist")

val database = getsharedprefrences("database", Context.MODE_PRIVET)
database.edit().apply{
    putstring("savedgettext", msgfromso.text.tostring )
}.apply
button2.setonclicklistner{
    
}